# For local development (with CUDA support), use:
# pip install -r requirements-local.txt
#
# For Docker (CPU-only), use:
# pip install -r requirements-docker.txt
#
# Default: Local development with CUDA
tensorflow[and-cuda]
tensorflow-datasets
mlflow
apache-airflow
streamlit
pillow
pandas
numpy
scikit-learn
matplotlib
seaborn
jupyter
ipykernel
# DVC with S3 support (for Backblaze B2)
dvc
boto3
s3fs
